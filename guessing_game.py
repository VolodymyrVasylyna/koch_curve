import turtle; def koch_curve(t, order, size): if order == 0: t.forward(size); else: for angle in [60, -120, 60, 0]: koch_curve(t, order-1, size/3); t.left(angle); turtle.speed(0); koch_curve(turtle, 4, 200); turtle.done()
